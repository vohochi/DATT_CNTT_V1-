// import { RootState } from '@/store';

// export const selectBannerGroups = (state: RootState) => state.banner.groups;
// export const selectBannerLoading = (state: RootState) => state.banner.status === 'loading';
// export const selectBannerError = (state: RootState) => state.banner.status === 'failed';
