/** @type {import('next').NextConfig} */
const nextConfig = {
  // Các cấu hình tùy chọn của Next.js
  reactStrictMode: true, // Ví dụ: bật chế độ Strict Mode của React
  // Nếu bạn muốn thêm cấu hình khác, ví dụ: rewrites, redirects, hoặc custom server

  // Bạn có thể sử dụng middleware ở đây
  // Chỉ áp dụng middleware cho API routes hoặc các routes khác

  // Cấu hình khác có thể thêm vào ở đây
};

export default nextConfig;
